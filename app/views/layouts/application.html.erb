<!DOCTYPE html>
<html>
<head>
  <title><%= t('site.title') %></title>
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="/"><%=t 'site.title' %></a>
      <div class="nav-collapse">
        <ul class="nav">
          <% if user_signed_in? %>
            <li><%= link_to t('event_occurrences.title'), event_occurrences_path %></li>
            <% if current_user.admin? %>
                <li><%= link_to t('menu.admin'), '/admin', :locale => nil %></li>
            <% end %>
          <% else %>

          <% end %>
          <%= render 'devise/menu/registration_items' %>
          <%= render 'devise/menu/login_items' %>        </ul>
      </div>
    </div>
  </div>
</div>


<div class="container">
  <div class="row">
    <div class="span12">
      <% if notice %>
          <p class="notice"><%= notice %></p>
      <% end %>
      <% if alert %>
          <p class="alert"><%= alert %></p>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="span9"><%= yield %></div>
    <div class="span3">
      <h2>Om systemet</h2>
      <p>Dette er en prototype på et meget simpelt event og tilmeldingssystem til KajakPiloterne.</p>
    </div>
  </div>
</div>

</body>
</html>
